# Frontend (React + Vite)

This React application uses **Vite** for a faster development experience. API requests are proxied to the backend server.

## Setup

```bash
cd frontend
npm install
npm run dev       # starts Vite on http://localhost:5173
```

The proxy configuration is found in `vite.config.js`. Build for production with `npm run build`.

